/**
 * Step 1 Route
 *
 * Personal data capture (gender, birth date).
 */

import { Step1PersonalScreen } from '@/features/onboarding';

export default function Step1() {
  return <Step1PersonalScreen />;
}
